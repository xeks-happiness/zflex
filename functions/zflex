# Main cli function of zflex
function zflex() {
	case "$1" in
		clone)
			shift
			$ZFLEX_HOME/scripts/clone "$@"
			;;
		source)
			shift
			_zflex_source "$@"; $ZFLEX_HOME/scripts/optimize
			;;
		update)
			shift
			$ZFLEX_HOME/scripts/update "$@"; $ZFLEX_HOME/scripts/optimize
			;;
	esac
}
